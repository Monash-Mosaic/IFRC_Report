name: Reusable — OSV Scan (PR)

on:
  workflow_call:

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  osv_scan_pr:
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable-pr.yml@v2.3.8
    with:
      fail-on-vuln: true
